import 'package:flutter/material.dart';

class PrintAgencyCardScreen extends StatelessWidget {
  const PrintAgencyCardScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(color: Colors.red),
    );
  }
}
